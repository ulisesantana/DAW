// Opera
if ($.browser.opera) {
  $(".yourclass").remove();
}

// Webkit (Google Chrome & Safari)
if ($.browser.webkit) {
  alert( "Este es webkit!" );
}

 // MSI
if ($.browser.msie) {
  alert( "Este es webkit!" );
}

  // Mozila
if ($.browser.mozilla) {
  alert( "Este es webkit!" );
}
